<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyThema" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/roxo_escuro</item>
        <item name="colorPrimaryDark">@color/roxo_padrao</item>
        <item name="colorAccent">@color/amarelo</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--<item name="android:windowFullscreen">true</item>-->
    </style>
    <style name="MyThemaSplahs" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/roxo_escuro</item>
        <item name="colorPrimaryDark">@color/roxo_padrao</item>
        <item name="colorAccent">@color/amarelo</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="noActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/roxo_escuro</item>
        <item name="colorPrimaryDark">@color/roxo_padrao</item>
        <item name="colorAccent">@color/amarelo</item>
        <item name="android:colorBackground">@color/branco</item>
        <item name="android:colorForeground">@color/branco</item>

    </style>

    <style name="MyThema.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="MyThema.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
